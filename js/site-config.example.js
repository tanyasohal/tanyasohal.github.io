/*!
 * Public site config (anon/publishable key only — safe in the browser).
 * Copy values from admin/js/config.js, then save as site-config.js
 */
window.SITE_CONFIG = {
  supabaseUrl: "YOUR_SUPABASE_URL",
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY"
};
